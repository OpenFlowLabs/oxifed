apiVersion: v1
kind: Service
metadata:
  name: {{ include "domainservd.fullname" . }}
  labels:
    {{- include "domainservd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domainservd.selectorLabels" . | nindent 4 }}
